{
  "name": "warden-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Warden — Portable Trust Infrastructure for AI Tools",
  "workspaces": [
    "packages/*",
    "apps/*",
    "samples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@warden/shared && npm run build --workspace=@warden/sdk && npm run build --workspace=@warden/api && npm run build --workspace=@warden/cli && npm run build --workspace=@warden/web && npm run build --workspace=@warden/demo-mcp-server",
    "dev:api": "npm --workspace=apps/api run dev",
    "dev:web": "npm --workspace=apps/web run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "cli": "node apps/cli/dist/index.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
